# ☁️ Облачный деплой - Доступ с телефона

Это руководство покажет, как развернуть UndetectBrowser в облаке и получить доступ с любого устройства (телефон, планшет, компьютер) из любой точки мира.

---

## 🚀 Быстрый выбор платформы

| Платформа | Сложность | Бесплатный план | Время деплоя | Рекомендация |
|-----------|-----------|-----------------|--------------|--------------|
| **Railway** | ⭐ Легко | $5 кредитов/мес | 3 мин | ✅ Лучший для начала |
| **Render** | ⭐⭐ Средне | Есть (ограничения) | 5 мин | ✅ Хорошая альтернатива |
| **Heroku** | ⭐⭐⭐ Сложнее | Нет | 10 мин | ⚠️ Требует оплаты |
| **DigitalOcean** | ⭐⭐⭐⭐ Сложно | $200 на 60 дней | 20 мин | 🔥 Для продакшена |
| **AWS/GCP** | ⭐⭐⭐⭐⭐ Очень сложно | Есть (сложный) | 30+ мин | ❌ Для экспертов |

**Рекомендация:** Начните с Railway или Render - это самые простые варианты.

---

## 🎯 Вариант 1: Railway.app (Рекомендуется)

### Почему Railway?
- ✅ **Самый простой деплой** - буквально 3 клика
- ✅ **Бесплатные $5 в месяц** (~500 часов работы)
- ✅ **Автоматический HTTPS**
- ✅ **Автообновления** из GitHub
- ✅ **Redis, PostgreSQL** из коробки
- ✅ **Logs и мониторинг** в реальном времени

### Шаги деплоя:

#### 1. Регистрация на Railway

**Перейдите:** https://railway.app

**Нажмите:** "Start a New Project"

**Войдите через GitHub** (рекомендуется) или Email

#### 2. Создание проекта

```
1. В Railway Dashboard нажмите "New Project"
2. Выберите "Deploy from GitHub repo"
3. Подключите ваш GitHub аккаунт (если еще не подключен)
4. Выберите репозиторий: wpeva/new-undetect-browser
5. Railway автоматически обнаружит настройки
```

#### 3. Настройка переменных окружения

**Railway автоматически добавит:**
- `PORT` (Railway назначит порт)
- `NODE_ENV=production`

**Добавьте вручную (опционально):**

В разделе "Variables" добавьте:

```env
# Основные
HOST=0.0.0.0
CORS_ORIGIN=*

# Безопасность (важно!)
JWT_SECRET=ваш-случайный-секретный-ключ-минимум-32-символа
ENABLE_AUTH=true

# Browser
BROWSER_HEADLESS=true
MAX_CONCURRENT_SESSIONS=5

# Логи
LOG_LEVEL=info
```

💡 **Генерация JWT_SECRET:**
```bash
# В терминале выполните:
node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
```

#### 4. Деплой

Railway автоматически:
1. ✅ Установит зависимости (`npm install`)
2. ✅ Скомпилирует TypeScript (`npm run build`)
3. ✅ Запустит сервер (`npm run server`)

**Время деплоя:** ~2-3 минуты

#### 5. Получение публичного URL

```
1. Перейдите в "Settings" вашего сервиса
2. Нажмите "Generate Domain"
3. Railway создаст URL типа:
   https://your-app-name.up.railway.app
```

**Готово!** 🎉 Теперь можно открыть этот URL с телефона!

---

### 📱 Как использовать с телефона

**iOS:**
1. Откройте Safari
2. Введите URL: `https://your-app.up.railway.app`
3. Нажмите "Поделиться" → "На экран Домой"
4. Теперь работает как приложение!

**Android:**
1. Откройте Chrome
2. Введите URL: `https://your-app.up.railway.app`
3. Меню (3 точки) → "Добавить на главный экран"
4. Теперь работает как приложение!

---

### 🔄 Автоматические обновления

Railway автоматически деплоит при каждом push в GitHub:

```bash
# Внесите изменения локально
git add .
git commit -m "Update feature"
git push origin main

# Railway автоматически задеплоит новую версию
```

---

### 📊 Мониторинг и логи

**В Railway Dashboard:**

1. **Logs** - Просмотр логов в реальном времени
2. **Metrics** - CPU, память, сеть
3. **Deployments** - История деплоев
4. **Usage** - Расход кредитов

---

### 💰 Цены Railway

| План | Цена | Ресурсы | Для чего |
|------|------|---------|----------|
| **Trial** | $5 кредитов бесплатно | 512MB RAM, 1 vCPU | Тестирование |
| **Hobby** | $5/месяц | До 8GB RAM, shared CPU | Личные проекты |
| **Pro** | Pay as you go | Неограниченно | Продакшен |

**Подсчет расходов:**
- $0.000231/минуту = ~$10/месяц для 24/7 работы
- Бесплатные $5 = ~500 часов (~20 дней)

---

## 🔷 Вариант 2: Render.com

### Почему Render?
- ✅ Бесплатный план (с ограничениями)
- ✅ Автоматический HTTPS
- ✅ База данных PostgreSQL бесплатно
- ⚠️ Засыпает после 15 мин неактивности (на бесплатном плане)

### Шаги деплоя:

#### 1. Регистрация

**Перейдите:** https://render.com

**Нажмите:** "Get Started for Free"

**Войдите через GitHub** или Email

#### 2. Создание Web Service

```
1. Dashboard → "New +"
2. Выберите "Web Service"
3. Подключите GitHub репозиторий
4. Выберите: wpeva/new-undetect-browser
```

#### 3. Настройки сервиса

**Name:** `undetect-browser` (или своё)

**Environment:** `Node`

**Build Command:**
```bash
npm install && npm run build && cd frontend && npm install && npm run build
```

**Start Command:**
```bash
npm run server
```

**Plan:** `Free` (или `Starter` за $7/мес для 24/7)

#### 4. Переменные окружения

В разделе "Environment Variables" добавьте:

```env
NODE_ENV=production
PORT=3000
HOST=0.0.0.0
CORS_ORIGIN=*
JWT_SECRET=ваш-секретный-ключ
BROWSER_HEADLESS=true
```

#### 5. Деплой

**Нажмите:** "Create Web Service"

Render автоматически деплоит проект.

**Время деплоя:** ~5-7 минут

**URL:** `https://your-app-name.onrender.com`

---

### ⚠️ Ограничения бесплатного плана Render

1. **Автоспящий режим** - сервис засыпает после 15 мин без запросов
2. **Медленный старт** - первый запрос после сна ~30-60 сек
3. **750 часов/месяц** - достаточно для одного сервиса 24/7

**Решение:**
- Используйте платный план Starter ($7/мес) для постоянной работы
- Или Railway (без автосна)

---

## 🟣 Вариант 3: Heroku

### Почему Heroku?
- ✅ Стабильная платформа
- ✅ Много дополнений (Redis, PostgreSQL, и т.д.)
- ❌ Нет бесплатного плана (с ноября 2022)
- ⚠️ Минимум $7/месяц

### Шаги деплоя:

#### 1. Установка Heroku CLI

**macOS:**
```bash
brew tap heroku/brew && brew install heroku
```

**Windows:**
Скачайте установщик: https://devcenter.heroku.com/articles/heroku-cli

**Linux:**
```bash
curl https://cli-assets.heroku.com/install.sh | sh
```

#### 2. Логин и создание приложения

```bash
# Логин
heroku login

# Перейдите в проект
cd new-undetect-browser

# Создайте приложение
heroku create your-app-name

# Добавьте buildpack для Node.js
heroku buildpacks:set heroku/nodejs
```

#### 3. Настройка переменных

```bash
heroku config:set NODE_ENV=production
heroku config:set HOST=0.0.0.0
heroku config:set CORS_ORIGIN=*
heroku config:set JWT_SECRET=$(node -e "console.log(require('crypto').randomBytes(32).toString('hex'))")
heroku config:set BROWSER_HEADLESS=true
```

#### 4. Деплой

```bash
# Добавьте Heroku remote (если не добавлен)
heroku git:remote -a your-app-name

# Деплой
git push heroku main

# Откройте приложение
heroku open
```

#### 5. Логи и мониторинг

```bash
# Просмотр логов
heroku logs --tail

# Информация о приложении
heroku ps

# Открыть dashboard
heroku open --app your-app-name
```

**URL:** `https://your-app-name.herokuapp.com`

---

### 💰 Цены Heroku

| Dyno | Цена | Ресурсы |
|------|------|---------|
| **Eco** | $5/мес | 512MB RAM, засыпает после 30 мин |
| **Basic** | $7/мес | 512MB RAM, 24/7 |
| **Standard-1X** | $25/мес | 512MB RAM, 24/7, автомасштабирование |

**Дополнения:**
- Heroku Postgres (бесплатно до 10k строк)
- Heroku Redis (от $0)

---

## 🌊 Вариант 4: DigitalOcean (VPS)

### Почему DigitalOcean?
- 🔥 Полный контроль
- 🔥 Максимальная производительность
- 🔥 $200 кредитов на 60 дней (новые аккаунты)
- ⚠️ Требует знаний Linux

### Быстрый деплой:

#### 1. Создание Droplet

**Перейдите:** https://cloud.digitalocean.com/droplets/new

**Выберите:**
- **Image:** Ubuntu 22.04 LTS
- **Plan:** Basic ($6/мес, 1 vCPU, 1GB RAM)
- **Datacenter:** Ближайший к вам
- **Authentication:** SSH keys (рекомендуется) или Password

**Создайте Droplet**

#### 2. Подключение по SSH

```bash
ssh root@your-droplet-ip
```

#### 3. Быстрая установка (копируй-пасть)

```bash
#!/bin/bash

# Обновление системы
apt update && apt upgrade -y

# Установка Node.js 20
curl -fsSL https://deb.nodesource.com/setup_20.x | bash -
apt install -y nodejs

# Установка Git
apt install -y git

# Клонирование проекта
cd /opt
git clone https://github.com/wpeva/new-undetect-browser.git
cd new-undetect-browser

# Установка зависимостей
npm install
cd frontend && npm install && cd ..

# Компиляция
npm run build

# Создание .env
cat > .env << EOF
NODE_ENV=production
PORT=3000
HOST=0.0.0.0
CORS_ORIGIN=*
JWT_SECRET=$(node -e "console.log(require('crypto').randomBytes(32).toString('hex'))")
BROWSER_HEADLESS=true
MAX_CONCURRENT_SESSIONS=20
EOF

# Установка PM2 для управления процессом
npm install -g pm2

# Запуск сервера
pm2 start npm --name "undetect-browser" -- run server

# Автозапуск при перезагрузке
pm2 startup
pm2 save

# Установка Nginx (опционально)
apt install -y nginx

# Настройка Nginx
cat > /etc/nginx/sites-available/undetect-browser << EOF
server {
    listen 80;
    server_name your-domain.com;  # Замените на ваш домен или IP

    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade \$http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host \$host;
        proxy_cache_bypass \$http_upgrade;
    }
}
EOF

# Активация конфига
ln -s /etc/nginx/sites-available/undetect-browser /etc/nginx/sites-enabled/
nginx -t
systemctl restart nginx

# Настройка firewall
ufw allow 22
ufw allow 80
ufw allow 443
ufw --force enable

echo "========================================="
echo "✅ Установка завершена!"
echo "========================================="
echo ""
echo "URL: http://$(curl -s ifconfig.me)"
echo ""
echo "Управление:"
echo "  pm2 status       - Статус"
echo "  pm2 logs         - Логи"
echo "  pm2 restart all  - Перезапуск"
echo "  pm2 stop all     - Остановка"
echo ""
```

#### 4. Доступ

**URL:** `http://your-droplet-ip:3000`

или с Nginx: `http://your-droplet-ip`

---

### 🔒 Настройка HTTPS (Let's Encrypt)

```bash
# Установка Certbot
apt install -y certbot python3-certbot-nginx

# Получение SSL сертификата
certbot --nginx -d your-domain.com

# Автоматическое обновление
certbot renew --dry-run
```

Теперь доступ по: `https://your-domain.com`

---

### 📊 Управление через PM2

```bash
# Статус всех процессов
pm2 status

# Логи в реальном времени
pm2 logs undetect-browser

# Перезапуск
pm2 restart undetect-browser

# Остановка
pm2 stop undetect-browser

# Удаление из PM2
pm2 delete undetect-browser

# Мониторинг
pm2 monit
```

---

## 📱 Универсальные инструкции для телефона

После деплоя на любую платформу:

### iOS (Safari)

1. Откройте URL в Safari
2. Нажмите кнопку "Поделиться" (квадрат со стрелкой вверх)
3. Прокрутите и нажмите "На экран Домой"
4. Введите название (например, "UndetectBrowser")
5. Нажмите "Добавить"

**Готово!** Теперь у вас иконка приложения на главном экране.

### Android (Chrome)

1. Откройте URL в Chrome
2. Нажмите меню (3 точки) → "Добавить на главный экран"
3. Введите название
4. Нажмите "Добавить"

**Готово!** Иконка появится на главном экране.

---

## 🔍 Проверка работы

После деплоя откройте в браузере:

```
https://your-app-url.com/api/health
```

**Должно вернуть:**
```json
{
  "status": "ok",
  "timestamp": "2024-01-24T12:00:00.000Z"
}
```

---

## ⚡ Рекомендации по выбору

### Для тестирования и личных проектов:
→ **Railway.app** - Самый простой, $5 бесплатно

### Для малого бизнеса:
→ **Render.com Starter** ($7/мес) или **Railway Hobby** ($5/мес)

### Для продакшена с высокой нагрузкой:
→ **DigitalOcean VPS** ($12+/мес) с PM2 и Nginx

### Для enterprise:
→ **AWS/GCP** + **Kubernetes** (см. `/kubernetes/README.md`)

---

## 🆘 Troubleshooting

### Проблема: "Application Error" после деплоя

**Решение:**
1. Проверьте логи: Railway/Render/Heroku логи
2. Убедитесь, что `PORT` переменная установлена правильно
3. Проверьте, что все зависимости установлены

### Проблема: "Cannot find module"

**Решение:**
```bash
# Очистите кэш и переустановите
rm -rf node_modules package-lock.json
npm install
npm run build
```

### Проблема: "Puppeteer не работает в облаке"

**Решение:** Убедитесь, что `BROWSER_HEADLESS=true` установлено

Для Railway/Render может потребоваться добавить buildpack для Chromium.

---

## 📚 Дополнительные ресурсы

- **Railway Documentation:** https://docs.railway.app
- **Render Documentation:** https://render.com/docs
- **Heroku Node.js Guide:** https://devcenter.heroku.com/articles/deploying-nodejs
- **DigitalOcean Tutorials:** https://www.digitalocean.com/community/tutorials

---

## ✅ Чеклист успешного деплоя

- [ ] Проект задеплоен на платформу
- [ ] Получен публичный URL
- [ ] `/api/health` возвращает 200 OK
- [ ] Frontend открывается в браузере
- [ ] Можно создать browser profile через UI
- [ ] Переменные окружения настроены
- [ ] HTTPS включен (для продакшена)
- [ ] Добавлено на главный экран телефона
- [ ] Работает с мобильного устройства

---

**🎉 Поздравляем!** Теперь ваш UndetectBrowser доступен из любой точки мира!
