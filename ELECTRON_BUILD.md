# 🛠️ Сборка Windows .exe клиента

## Быстрая сборка

```bash
# 1. Установите зависимости (если еще не установлены)
npm install

# 2. Соберите TypeScript
npm run build

# 3. Соберите .exe для Windows
npm run electron:build
```

**Готовый установщик будет в папке:** `release/UndetectBrowser-Setup-1.0.0.exe`

---

## Детальные инструкции

### Шаг 1: Установка зависимостей

```bash
npm install
```

Это установит:
- Electron (~200 MB)
- Electron Builder
- Все остальные зависимости

### Шаг 2: Компиляция TypeScript

```bash
npm run build
```

Компилирует:
- `src/**/*.ts` → `dist/**/*.js`
- `electron/**/*.ts` → `dist/electron/**/*.js`

### Шаг 3: Тестирование в режиме разработки

```bash
npm run electron:dev
```

Запустит приложение локально для тестирования.

### Шаг 4: Сборка финального .exe

```bash
npm run electron:build
```

Создаст:
- `release/UndetectBrowser-Setup-1.0.0.exe` - Установщик NSIS
- `release/win-unpacked/` - Распакованная версия

---

## Настройка сборки

### Изменить иконку приложения

1. Создайте иконку `.ico` (256x256 px)
2. Поместите в `build/icon.ico`
3. Пересоберите: `npm run electron:build`

### Изменить название приложения

Отредактируйте `package.json`:

```json
{
  "name": "undetect-browser",
  "productName": "UndetectBrowser",
  "build": {
    "productName": "UndetectBrowser"
  }
}
```

### Настроить установщик NSIS

В `package.json`:

```json
{
  "build": {
    "nsis": {
      "oneClick": false,              // Показывать мастер установки
      "allowToChangeInstallationDirectory": true,
      "createDesktopShortcut": true,  // Ярлык на рабочем столе
      "createStartMenuShortcut": true // Ярлык в меню Пуск
    }
  }
}
```

---

## Сборка для разных платформ

### Windows (текущая система)

```bash
npm run electron:build
```

### Все платформы (Windows + Mac + Linux)

```bash
npm run electron:build:all
```

**Примечание:** Для сборки Mac приложения на Windows нужен macOS.

---

## Размер приложения

Ожидаемые размеры:

- **Установщик (.exe):** ~150-200 MB
- **Установленное приложение:** ~300-400 MB
- **В памяти (1 профиль):** ~300-500 MB

Размер большой из-за:
- Chromium (внутри Puppeteer)
- Node.js runtime
- Electron framework

---

## Оптимизация размера

### 1. Исключить ненужные файлы

В `package.json`:

```json
{
  "build": {
    "files": [
      "dist/**/*",
      "electron/**/*",
      "!**/*.test.js",
      "!**/*.spec.js"
    ]
  }
}
```

### 2. Использовать asar архив

```json
{
  "build": {
    "asar": true
  }
}
```

### 3. Включить сжатие

```json
{
  "build": {
    "compression": "maximum"
  }
}
```

---

## Подписание приложения (Windows)

Для продакшн релиза нужно подписать приложение сертификатом:

```json
{
  "build": {
    "win": {
      "certificateFile": "path/to/cert.pfx",
      "certificatePassword": "password"
    }
  }
}
```

**Получить сертификат:**
- DigiCert
- Sectigo
- GlobalSign

**Стоимость:** ~$200-500/год

---

## Автоматическая сборка (CI/CD)

### GitHub Actions

Создайте `.github/workflows/build.yml`:

```yaml
name: Build Electron App

on:
  push:
    tags:
      - 'v*'

jobs:
  build-windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v2
        with:
          node-version: '18'
      - run: npm install
      - run: npm run build
      - run: npm run electron:build
      - uses: actions/upload-artifact@v2
        with:
          name: windows-installer
          path: release/*.exe
```

---

## Troubleshooting

### Ошибка: "electron-builder not found"

```bash
npm install --save-dev electron-builder
```

### Ошибка: "ENOENT: no such file or directory"

Убедитесь, что запустили `npm run build` перед `electron:build`

### Ошибка: Icon not found

Создайте файл `build/icon.ico` или отключите иконку:

```json
{
  "build": {
    "win": {
      "icon": null
    }
  }
}
```

### Приложение не запускается

Проверьте:
1. Установлен ли Node.js 18+
2. Скомпилированы ли TypeScript файлы (`npm run build`)
3. Логи в `%APPDATA%/UndetectBrowser/logs/`

---

## Рекомендации по релизу

### Pre-release чеклист:

- [ ] Обновлена версия в `package.json`
- [ ] Пройдены все тесты: `npm test`
- [ ] Проверен линтер: `npm run lint`
- [ ] Собран проект: `npm run build`
- [ ] Протестирован в dev режиме: `npm run electron:dev`
- [ ] Создан .exe: `npm run electron:build`
- [ ] Протестирован установщик на чистой системе
- [ ] Обновлена документация
- [ ] Созданы release notes

### Релиз на GitHub:

```bash
# 1. Создайте тег
git tag v1.0.0
git push origin v1.0.0

# 2. Создайте GitHub Release
# 3. Загрузите .exe файл из release/
# 4. Опубликуйте
```

---

## Поддержка пользователей

### Системные требования (включите в README)

**Минимальные:**
- Windows 10 64-bit
- 4 GB RAM
- 500 MB свободного места

**Рекомендуемые:**
- Windows 10/11 64-bit
- 8 GB RAM
- 2 GB свободного места

### Первый запуск

1. Установите приложение
2. Запустите через ярлык на рабочем столе
3. Создайте первый профиль
4. Укажите прокси (опционально)
5. Нажмите "Запустить"

---

**Готово! Теперь у вас есть полноценный Windows клиент!** 🎉
