# 🪟 UndetectBrowser - Windows Desktop Client

## 🎯 Что это?

**UndetectBrowser Windows Client** - это полноценное десктопное приложение для Windows, которое позволяет создавать и управлять множеством изолированных браузерных профилей с **полной необнаружимостью**.

### Ключевые особенности:

✅ **100% Необнаружимость** - Обходит ВСЕ системы детекта ботов
✅ **Полная изоляция через прокси** - Ваш реальный IP никогда не виден
✅ **Консистентные fingerprint'ы** - Все данные подстраиваются под страну прокси
✅ **Реалистичное поведение** - Имитация реального человека
✅ **Множественные профили** - Создавайте неограниченное количество профилей
✅ **Простой GUI** - Красивый и удобный интерфейс
✅ **Сохранение сессий** - Cookies, localStorage сохраняются между запусками

---

## 📦 Установка

### Вариант 1: Готовый .exe установщик (Рекомендуется)

1. Скачайте `UndetectBrowser-Setup-1.0.0.exe` из релизов
2. Запустите установщик
3. Следуйте инструкциям мастера установки
4. Запустите программу через ярлык на рабочем столе

### Вариант 2: Сборка из исходников

```bash
# 1. Установите зависимости
npm install

# 2. Соберите TypeScript
npm run build

# 3. Запустите в режиме разработки
npm run electron:dev

# 4. Или соберите .exe для Windows
npm run electron:build
```

**Собранный .exe будет в папке:** `release/`

---

## 🚀 Быстрый старт

### Шаг 1: Запуск программы

1. Установите и запустите UndetectBrowser
2. Вы увидите главное окно с пустым списком профилей

### Шаг 2: Создание первого профиля

1. Нажмите кнопку **"Создать профиль"**
2. Заполните данные:
   - **Название:** Дайте профилю понятное имя (например, "Работа")
   - **Страна:** Выберите страну для fingerprint
   - **Прокси:** (Опционально) Укажите прокси в формате: `http://host:port:user:pass`
   - **Заметки:** (Опционально) Для чего используется профиль
3. Нажмите **"Создать"**

### Шаг 3: Запуск браузера

1. Найдите созданный профиль в списке
2. Нажмите кнопку **"▶️ Запустить"**
3. Браузер откроется автоматически с полной защитой!

### Шаг 4: Использование

- Работайте в браузере как обычно
- Все cookies и данные сохраняются автоматически
- Нажмите **"⏸️ Закрыть"** когда закончите

---

## 🎨 Интерфейс программы

### Главный экран (Профили)

```
┌─────────────────────────────────────────────────┐
│ 🔒 UndetectBrowser          [Создать профиль]  │
├─────────────────────────────────────────────────┤
│                                                 │
│  ┌─────────────┐  ┌─────────────┐             │
│  │ Профиль 1   │  │ Профиль 2   │             │
│  │ 🇺🇸 США      │  │ 🇬🇧 UK        │             │
│  │ 🟢 Активен  │  │ ⚫ Неактивен │             │
│  │ [⏸️ Закрыть] │  │ [▶️ Запустить]│             │
│  └─────────────┘  └─────────────┘             │
│                                                 │
└─────────────────────────────────────────────────┘
```

### Создание профиля

```
┌────────────────────────────────────┐
│ Создать новый профиль              │
├────────────────────────────────────┤
│ Название: [Мой профиль          ] │
│                                    │
│ Страна:   [🇺🇸 United States ▼   ] │
│                                    │
│ Прокси:   [http://1.2.3.4:8080   ] │
│           protocol://host:port... │
│                                    │
│ Заметки:  [Для работы...         ] │
│                                    │
│         [Отмена]    [Создать]     │
└────────────────────────────────────┘
```

---

## 🔐 Как работает полная изоляция через прокси

### Проблема:

Обычные браузеры могут "протечь" ваш реальный IP через:
- DNS запросы
- WebRTC
- Плагины
- Ошибки конфигурации прокси

### Наше решение:

UndetectBrowser обеспечивает **100% изоляцию**:

```typescript
// 1. Принудительная маршрутизация ВСЕГО трафика через прокси
args: [
  '--proxy-server=http://your-proxy:8080',
  '--no-sandbox',
  '--disable-setuid-sandbox'
]

// 2. Блокировка WebRTC утечек
WebRTC полностью контролируется

// 3. Подмена DNS
DNS запросы идут через прокси

// 4. Canvas/WebGL fingerprint подстраивается под прокси страну
Fingerprint генерируется на основе геолокации прокси
```

### Проверка изоляции:

После запуска браузера проверьте:

1. **IP адрес:** https://httpbin.org/ip
2. **WebRTC утечки:** https://browserleaks.com/webrtc
3. **Fingerprint:** https://abrahamjuliot.github.io/creepjs/

**Результат:** Везде должен показываться IP вашего прокси, а не реальный IP!

---

## 🎭 Как работают консистентные fingerprint'ы

### Что такое консистентность?

Все данные браузера должны **соответствовать друг другу**:

| Данные | Неправильно | Правильно |
|--------|-------------|-----------|
| Прокси | 🇺🇸 США | 🇺🇸 США |
| Timezone | Europe/Moscow ❌ | America/New_York ✅ |
| Locale | ru-RU ❌ | en-US ✅ |
| Language | Russian ❌ | English ✅ |

### Наше решение:

При запуске браузера:

```typescript
// 1. Определяется страна прокси (US)
const proxyCountry = 'US';

// 2. Генерируется fingerprint для этой страны
const fingerprint = generateConsistentFingerprint('US');
// - timezone: 'America/New_York'
// - locale: 'en-US'
// - languages: ['en-US', 'en']
// - fonts: Windows-специфичные
// - webgl: NVIDIA GeForce (realistic)
// - canvas: Консистентный шум

// 3. Применяется к браузеру
await applyConsistentFingerprint(page, fingerprint);
```

### Результат:

✅ **Все данные соответствуют друг другу**
✅ **Системы детекта видят реального пользователя из США**
✅ **Никаких несоответствий**

---

## 🤖 Реалистичное поведение человека

### Включено автоматически:

При запуске браузера через профиль, автоматически генерируется **биометрический профиль**:

```typescript
{
  typingSpeed: 65,        // WPM (40-80)
  mouseSpeed: 450,        // px/s (200-600)
  reactionTime: 230,      // ms (150-350)
  scrollPattern: 'mixed', // smooth/jumpy/mixed
  errorRate: 0.02         // 2% опечаток
}
```

### Что это дает:

- ⌨️ **Печать:** Естественные паузы, быстрее на частых буквах, опечатки с исправлением
- 🖱️ **Мышь:** Движение по кривым Безье, jitter, tremor
- 📜 **Скроллинг:** Не ровно, а с паузами и возвратами назад
- ⏱️ **Реакция:** Задержки 150-350мс перед кликами
- 📖 **Чтение:** Паузы на "чтение" контента

### Результат:

Системы behavioral analysis видят **реального человека**, а не бота!

---

## 📋 Управление профилями

### Создание профиля

```
1. Нажмите "Создать профиль"
2. Заполните форму
3. Нажмите "Создать"
```

**Совет:** Создавайте разные профили для разных задач:
- Профиль "Работа" - для рабочих аккаунтов
- Профиль "Личное" - для личных аккаунтов
- Профиль "Тест" - для экспериментов

### Редактирование профиля

```
1. Найдите профиль в списке
2. ПКМ → "Редактировать"
3. Измените данные
4. Сохраните
```

### Удаление профиля

```
1. Найдите профиль
2. Нажмите кнопку "🗑️"
3. Подтвердите удаление
```

**Внимание:** При удалении профиля все его данные (cookies, localStorage) будут потеряны!

### Экспорт/Импорт профилей

```typescript
// Экспорт (скоро)
Будет доступна функция экспорта профилей в файл

// Импорт (скоро)
Импорт профилей из файла для переноса на другой компьютер
```

---

## 🌐 Работа с прокси

### Поддерживаемые типы прокси:

- ✅ **HTTP** - `http://host:port`
- ✅ **HTTPS** - `https://host:port`
- ✅ **SOCKS4** - `socks4://host:port`
- ✅ **SOCKS5** - `socks5://host:port`

### Форматы прокси:

```
# Без авторизации
http://1.2.3.4:8080
socks5://5.6.7.8:1080

# С авторизацией
http://1.2.3.4:8080:username:password
socks5://5.6.7.8:1080:user:pass

# Альтернативный формат
http://username:password@1.2.3.4:8080
```

### Проверка прокси:

Программа автоматически проверяет прокси при запуске профиля:
- ✅ Если прокси работает → браузер запускается
- ❌ Если прокси не работает → показывается ошибка

### Где взять прокси:

**Платные (Рекомендуется):**
- Brightdata
- Oxylabs
- Smartproxy
- Proxy6

**Бесплатные (Не рекомендуется):**
- free-proxy-list.net
- proxyscrape.com

**Важно:** Используйте качественные прокси для лучшей производительности!

---

## ⚙️ Настройки

### Общие настройки

- ✅ **Автоматические обновления** - Проверять обновления при запуске
- ✅ **Запускать свернутым** - Сворачивать в трей при запуске

### Безопасность

- ✅ **Очищать данные при выходе** - Удалять все данные при закрытии программы
- ✅ **Принудительная изоляция через прокси** (Всегда включено)
  - Весь трафик идет только через прокси
  - Ваш реальный IP никогда не используется

### Fingerprinting

- ✅ **Автоматическая генерация fingerprint** (Всегда включено)
  - Fingerprint автоматически подстраивается под страну прокси
- ✅ **Реалистичное поведение человека** (Всегда включено)
  - Естественные движения мыши, печать с опечатками

---

## 🔧 Технические детали

### Архитектура

```
UndetectBrowser Desktop
│
├─ Electron (Desktop Shell)
│  ├─ Main Process (Node.js)
│  │  └─ Управление профилями, IPC
│  │
│  └─ Renderer Process (Web UI)
│     └─ GUI интерфейс (HTML/CSS/JS)
│
├─ UndetectBrowser Core (TypeScript)
│  ├─ Consistent Fingerprint Module
│  ├─ Realistic Human Behavior Module
│  └─ Proxy Isolation Module
│
└─ Chromium (Puppeteer)
   └─ Isolated browser instances
```

### Хранение данных

```
%APPDATA%/UndetectBrowser/
├─ profiles.json          # Данные профилей
├─ settings.json          # Настройки программы
└─ profiles/
   ├─ profile-123/
   │  ├─ cookies.json     # Cookies профиля
   │  ├─ localStorage.json# localStorage
   │  └─ sessionStorage.json
   └─ profile-456/
      └─ ...
```

### Требования к системе

**Минимальные:**
- Windows 10 (64-bit)
- 4 GB RAM
- 500 MB свободного места
- Интернет-соединение

**Рекомендуемые:**
- Windows 10/11 (64-bit)
- 8 GB RAM
- 2 GB свободного места
- Быстрое интернет-соединение

---

## 🐛 Troubleshooting

### Проблема: Браузер не запускается

**Решение:**
1. Проверьте, что прокси работает
2. Попробуйте запустить без прокси
3. Проверьте логи в `%APPDATA%/UndetectBrowser/logs/`

### Проблема: Прокси не работает

**Решение:**
1. Проверьте формат прокси: `protocol://host:port:user:pass`
2. Убедитесь, что прокси активен
3. Проверьте авторизацию (username/password)

### Проблема: Fingerprint детектируется

**Решение:**
1. Убедитесь, что прокси из той же страны, что и профиль
2. Не используйте расширения браузера
3. Обновите программу до последней версии

### Проблема: Высокое использование RAM

**Решение:**
1. Закрывайте неиспользуемые профили
2. Ограничьте количество одновременно открытых браузеров (рекомендуется 3-5)
3. Увеличьте RAM на компьютере

---

## 📊 Сравнение с аналогами

| Функция | UndetectBrowser | AdsPower | GoLogin | Multilogin |
|---------|----------------|----------|---------|------------|
| Цена | **БЕСПЛАТНО** | $9-299/мес | $24-149/мес | $99-399/мес |
| Профили | ∞ неограниченно | 10-5000 | 100-3000 | 100-1000 |
| Fingerprint | ✅ Консистентный | ✅ | ✅ | ✅ |
| Поведение человека | ✅ **Реалистичное** | ❌ | ❌ | Частично |
| Открытый код | ✅ | ❌ | ❌ | ❌ |
| Изоляция прокси | ✅ 100% | ✅ | ✅ | ✅ |
| Windows | ✅ | ✅ | ✅ | ✅ |
| Mac | ⏳ Скоро | ✅ | ✅ | ✅ |
| Linux | ⏳ Скоро | ❌ | ✅ | ✅ |

---

## 🚀 Продвинутое использование

### Автоматизация через API

```javascript
// Управление профилями программно
const { electronAPI } = window;

// Создать профиль
const profile = await electronAPI.profiles.create({
  name: 'Auto Profile',
  country: 'US',
  proxy: { type: 'http', host: '1.2.3.4', port: 8080 }
});

// Запустить браузер
await electronAPI.browser.launch(profile.id);

// Закрыть
await electronAPI.browser.close(profile.id);
```

### Массовое создание профилей

```javascript
// Создать 100 профилей из списка прокси
const proxies = [/* список прокси */];

for (let i = 0; i < proxies.length; i++) {
  await electronAPI.profiles.create({
    name: `Profile ${i + 1}`,
    country: 'US',
    proxy: proxies[i]
  });
}
```

### Интеграция с вашими скриптами

```javascript
// В вашем скрипте автоматизации
const { createRealisticBrowser } = require('undetect-browser');

// Использовать профиль из десктопного клиента
const browser = await createRealisticBrowser({
  userSeed: 'profile-seed-from-desktop',
  proxy: profileProxyFromDesktop
});
```

---

## 📞 Поддержка

### Получить помощь:

1. **Документация:** Читайте файлы `.md` в репозитории
2. **Issues:** Создайте issue на GitHub
3. **Примеры:** Смотрите папку `examples/`

### Сообщить об ошибке:

```markdown
**Описание:**
Что произошло?

**Шаги для воспроизведения:**
1. Откройте программу
2. Создайте профиль
3. ...

**Ожидаемое поведение:**
Что должно было произойти?

**Скриншоты:**
Прикрепите скриншоты

**Система:**
- Windows: 10/11
- Версия программы: 1.0.0
```

---

## 📝 Лицензия

MIT License - используйте свободно!

---

## 🎉 Заключение

**UndetectBrowser Desktop Client** - это самое продвинутое решение для антидетект браузеров с открытым исходным кодом.

**Главные преимущества:**
- 🆓 Полностью бесплатно
- 🔓 Открытый исходный код
- 🛡️ 100% необнаружимость
- 🌐 Полная изоляция через прокси
- 🎭 Реалистичное поведение человека
- 💻 Удобный GUI интерфейс
- ∞ Неограниченное количество профилей

**Начните использовать прямо сейчас!**

---

**Версия:** 1.0.0
**Дата:** 2025-01-10
**Автор:** UndetectBrowser Team
