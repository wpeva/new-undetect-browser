# ⚡ Railway - Быстрый старт (3 минуты)

## 🎯 Что нужно:
- GitHub аккаунт
- Ваш проект на GitHub

---

## 📝 Инструкция за 5 шагов:

### 1️⃣ Зайдите на Railway
```
https://railway.app
```
→ **"Login with GitHub"**

### 2️⃣ Создайте проект
→ **"New Project"**
→ **"Deploy from GitHub repo"**
→ Выберите: `wpeva/new-undetect-browser`

### 3️⃣ Добавьте переменные
Кликните на сервис → **"Variables"** → **"+ New Variable"**

**Скопируйте и вставьте:**
```env
NODE_ENV=production
HOST=0.0.0.0
CORS_ORIGIN=*
JWT_SECRET=ваш-случайный-секрет-32-символа
BROWSER_HEADLESS=true
MAX_CONCURRENT_SESSIONS=5
```

**💡 Генерация JWT_SECRET:**
```bash
node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
```

### 4️⃣ Настройте сборку
**Settings** → **"Build"**

**Build Command:**
```bash
npm install --legacy-peer-deps && npx tsc --skipLibCheck
```

**Start Command:**
```bash
node dist/server/index-v2.js
```

**✅ Нажмите "Deploy"**

### 5️⃣ Получите URL
**Settings** → **"Networking"** → **"Generate Domain"**

Получите:
```
https://your-app-xxxx.up.railway.app
```

---

## ✅ Готово!

Откройте на телефоне:
```
https://your-app-xxxx.up.railway.app
```

**Добавьте на главный экран:**
- **iOS:** Safari → Поделиться → "На экран Домой"
- **Android:** Chrome → Меню (⋮) → "Добавить на главный экран"

---

## ❌ Если не работает

### Ошибка сборки?
**Settings** → **"Build"** → Проверьте команды выше

### Application Error?
**Variables** → Проверьте все переменные

### Нужна детальная инструкция?
→ Откройте файл **`RAILWAY_DEPLOY_GUIDE.md`**

---

## 💰 Стоимость
- **Бесплатно:** $5 кредитов (~500 часов)
- **Hobby:** $5/месяц
- **Pro:** ~$10/месяц для 24/7

---

**Готово! 🚀 Ваше приложение в облаке!**
