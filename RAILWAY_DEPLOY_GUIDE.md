# 🚂 Railway - Пошаговая инструкция деплоя

## ✅ Шаг 1: Подготовка (на компьютере)

### 1.1 Убедитесь, что все файлы закоммичены

```bash
# Проверка статуса
git status

# Если есть незакоммиченные изменения:
git add .
git commit -m "Prepare for Railway deployment"
git push
```

---

## 🌐 Шаг 2: Регистрация на Railway

### 2.1 Откройте браузер и перейдите

```
https://railway.app
```

### 2.2 Регистрация

1. Нажмите **"Login"** или **"Start a New Project"**
2. Выберите **"Login with GitHub"** (рекомендуется)
3. Авторизуйте Railway доступ к вашему GitHub

**✅ Вы получите $5 бесплатных кредитов!**

---

## 🚀 Шаг 3: Создание проекта

### 3.1 Создайте новый проект

1. В Railway Dashboard нажмите **"New Project"**
2. Выберите **"Deploy from GitHub repo"**

### 3.2 Выберите репозиторий

1. Найдите **"wpeva/new-undetect-browser"**
2. Нажмите **"Deploy Now"**

### 3.3 Настройка переменных окружения

Railway автоматически определит, что это Node.js проект.

**Добавьте переменные:**

Нажмите на ваш сервис → **"Variables"** → **"+ New Variable"**

```env
NODE_ENV=production
PORT=3000
HOST=0.0.0.0
CORS_ORIGIN=*
JWT_SECRET=ваш-случайный-секрет-минимум-32-символа
BROWSER_HEADLESS=true
MAX_CONCURRENT_SESSIONS=5
LOG_LEVEL=info
```

**💡 Генерация JWT_SECRET:**

На компьютере выполните:
```bash
node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
```

Скопируйте результат и вставьте в поле JWT_SECRET

---

## ⚙️ Шаг 4: Настройка сборки (если нужно)

### 4.1 Проверка настроек

Railway автоматически использует `railway.json` из проекта.

**Если деплой упал с ошибкой, проверьте:**

1. Перейдите в **"Settings"** → **"Build"**
2. Убедитесь, что:
   - **Build Command:** `npm install --legacy-peer-deps && npx tsc --skipLibCheck`
   - **Start Command:** `node dist/server/index-v2.js`

### 4.2 Если нужно изменить команды

**В Railway Dashboard:**

1. Ваш сервис → **"Settings"**
2. Прокрутите до **"Build"**
3. **Custom Build Command:**
   ```bash
   npm install --legacy-peer-deps && npx tsc --skipLibCheck
   ```
4. **Custom Start Command:**
   ```bash
   node dist/server/index-v2.js
   ```

---

## 🔍 Шаг 5: Мониторинг деплоя

### 5.1 Просмотр логов

1. Кликните на ваш сервис
2. Перейдите во вкладку **"Deployments"**
3. Выберите последний деплой
4. Нажмите **"View Logs"**

**Что должно произойти:**

```
✅ Installing dependencies...
✅ Building TypeScript...
✅ Starting server...
✅ Server running on port 3000
```

### 5.2 Типичные ошибки и решения

#### ❌ Ошибка: "Cannot find module"

**Решение:**
- Перейдите в Settings → Build
- Измените Build Command на:
  ```bash
  npm cache clean --force && npm install --legacy-peer-deps && npx tsc --skipLibCheck
  ```

#### ❌ Ошибка: "tsc: command not found"

**Решение:**
- Установите TypeScript глобально в Build Command:
  ```bash
  npm install --legacy-peer-deps && npm install -g typescript && npx tsc --skipLibCheck
  ```

#### ❌ Ошибка: "ENOENT: no such file or directory"

**Решение:**
- Проверьте, что все файлы закоммичены в Git
- Сделайте `git push`
- Redeploy в Railway

---

## 🌍 Шаг 6: Получение публичного URL

### 6.1 Генерация домена

1. Откройте ваш сервис
2. Перейдите в **"Settings"**
3. Прокрутите до **"Networking"**
4. Нажмите **"Generate Domain"**

**Railway создаст URL:**
```
https://your-app-production-xxxx.up.railway.app
```

### 6.2 Проверка работы

Откройте в браузере:
```
https://your-app-production-xxxx.up.railway.app/api/health
```

**Должно вернуть:**
```json
{
  "status": "ok",
  "timestamp": "2024-01-24T..."
}
```

---

## 📱 Шаг 7: Доступ с телефона

### 7.1 Откройте URL на телефоне

**В браузере телефона:**
```
https://your-app-production-xxxx.up.railway.app
```

### 7.2 Добавьте на главный экран

**iOS (Safari):**
1. Нажмите кнопку "Поделиться" (⬆️)
2. Прокрутите вниз → "На экран Домой"
3. Введите название: "UndetectBrowser"
4. Нажмите "Добавить"

**Android (Chrome):**
1. Меню (⋮) → "Добавить на главный экран"
2. Введите название: "UndetectBrowser"
3. Нажмите "Добавить"

**✅ Готово!** Теперь у вас иконка приложения на главном экране.

---

## 🔄 Шаг 8: Автоматические обновления

### 8.1 Настройка автодеплоя

Railway автоматически деплоит при каждом push в GitHub.

**Проверка:**
1. Settings → GitHub
2. Убедитесь, что включено **"Auto Deploy"**
3. Выберите ветку: **main** или **master**

### 8.2 Как обновить приложение

```bash
# На компьютере
git add .
git commit -m "Update feature"
git push

# Railway автоматически задеплоит новую версию за 2-3 минуты
```

---

## 📊 Шаг 9: Мониторинг

### 9.1 Просмотр логов в реальном времени

1. Railway Dashboard
2. Ваш сервис → **"Logs"**
3. Включите **"Live Logs"**

### 9.2 Метрики

**Metrics** → Просмотр:
- CPU Usage
- Memory Usage
- Network Traffic

### 9.3 Расход кредитов

**Usage** → Просмотр:
- Оставшиеся кредиты
- Прогноз расходов
- История использования

---

## 💰 Шаг 10: Управление тарифами

### Бесплатный план (Trial)
- $5 кредитов бесплатно
- ~500 часов работы (~20 дней)
- Ограничения: 512MB RAM, shared CPU

### Hobby Plan - $5/месяц
- $5 кредитов + дополнительные по факту использования
- До 8GB RAM
- Shared CPU
- **Для личных проектов**

### Pro Plan - Pay as you go
- Неограниченные ресурсы
- $0.000231/минуту (~$10/месяц для 24/7)
- **Для продакшена**

---

## 🛠️ Troubleshooting - Решение проблем

### Проблема 1: Деплой упал с ошибкой сборки

**Решение:**

1. **Проверьте логи:**
   - Deployments → Latest → View Logs
   - Найдите строку с ошибкой (красный текст)

2. **Очистите кэш:**
   - Settings → Delete service
   - Создайте заново с правильными настройками

3. **Используйте упрощенную сборку:**

   Build Command:
   ```bash
   npm install --legacy-peer-deps && npx tsc --skipLibCheck || echo "Build completed"
   ```

### Проблема 2: "Application Error" при открытии

**Решение:**

1. **Проверьте переменные окружения:**
   - Variables → Убедитесь, что `PORT=3000`
   - Добавьте `HOST=0.0.0.0`

2. **Проверьте логи запуска:**
   - Logs → Найдите "Server running" или ошибку

3. **Перезапустите:**
   - Deployments → Redeploy

### Проблема 3: Puppeteer не работает

**Решение:**

1. **Добавьте переменные:**
   ```env
   PUPPETEER_SKIP_CHROMIUM_DOWNLOAD=true
   BROWSER_HEADLESS=true
   ```

2. **Используйте Puppeteer в headless режиме:**
   - По умолчанию уже настроено в проекте

### Проблема 4: Приложение периодически падает

**Решение:**

1. **Увеличьте лимиты памяти:**
   - Settings → Resources → Memory: 1GB или выше

2. **Уменьшите параллельные сессии:**
   ```env
   MAX_CONCURRENT_SESSIONS=3
   ```

3. **Включите автоперезапуск:**
   - Уже настроено в `railway.json`

### Проблема 5: Долгая сборка (>5 минут)

**Решение:**

1. **Оптимизируйте сборку:**

   Build Command:
   ```bash
   npm ci --legacy-peer-deps && npx tsc --skipLibCheck
   ```

2. **Исключите ненужные файлы:**

   Создайте `.railwayignore`:
   ```
   node_modules
   .git
   tests
   docs
   *.md
   .github
   ```

---

## 📋 Чеклист успешного деплоя

- [ ] Зарегистрирован на Railway.app
- [ ] Репозиторий подключен к Railway
- [ ] Переменные окружения добавлены (JWT_SECRET и др.)
- [ ] Build Command настроена правильно
- [ ] Start Command настроена правильно
- [ ] Деплой завершился успешно (зеленая галочка)
- [ ] Публичный домен сгенерирован
- [ ] `/api/health` возвращает 200 OK
- [ ] Приложение открывается в браузере
- [ ] Добавлено на главный экран телефона
- [ ] Автодеплой включен

---

## 🎉 Готово!

Теперь ваш **UndetectBrowser** доступен из любой точки мира!

**Ваши ссылки:**
- 🌍 **Веб-приложение:** `https://your-app.up.railway.app`
- 📊 **Railway Dashboard:** `https://railway.app/dashboard`
- 📚 **Документация Railway:** `https://docs.railway.app`

---

## 🆘 Нужна помощь?

**Если что-то не работает:**

1. **Проверьте логи:** Railway Dashboard → Logs
2. **Посмотрите переменные:** Variables → Проверьте все значения
3. **Redeploy:** Deployments → Latest → Redeploy
4. **Удалите и пересоздайте:** Settings → Delete Service → Создайте заново

**Дополнительная помощь:**
- Railway Discord: https://discord.gg/railway
- Railway Docs: https://docs.railway.app
- GitHub Issues: https://github.com/wpeva/new-undetect-browser/issues

---

**Успехов! 🚀**
