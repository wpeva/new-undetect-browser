diff --git a/third_party/blink/renderer/modules/webaudio/analyser_node.cc b/third_party/blink/renderer/modules/webaudio/analyser_node.cc
index abcdef1..1234567 100644
--- a/third_party/blink/renderer/modules/webaudio/analyser_node.cc
+++ b/third_party/blink/renderer/modules/webaudio/analyser_node.cc
@@ -28,6 +28,7 @@
 #include "third_party/blink/renderer/modules/webaudio/audio_node_output.h"
 #include "third_party/blink/renderer/modules/webaudio/base_audio_context.h"
 #include "third_party/blink/renderer/platform/bindings/exception_state.h"
+#include <random>

 namespace blink {

@@ -180,6 +181,35 @@ void AnalyserHandler::Process(uint32_t frames_to_process) {
   output_bus->CopyFrom(*input_bus);
 }

+// Audio Fingerprint Protection: Add subtle noise to audio data
+static uint64_t GetAudioSessionSeed() {
+  static uint64_t seed = std::chrono::system_clock::now().time_since_epoch().count();
+  return seed;
+}
+
+static void AddAudioNoise(float* data, size_t size) {
+  static thread_local std::mt19937_64 gen(GetAudioSessionSeed());
+  std::uniform_real_distribution<float> dist(-0.0001f, 0.0001f);
+
+  for (size_t i = 0; i < size; i++) {
+    data[i] += dist(gen);
+  }
+}
+
+static void AddByteNoise(uint8_t* data, size_t size) {
+  static thread_local std::mt19937_64 gen(GetAudioSessionSeed());
+  std::uniform_int_distribution<int> dist(-1, 1);
+
+  // Only modify ~1% of values
+  size_t noise_count = size / 100;
+  for (size_t i = 0; i < noise_count; i++) {
+    size_t idx = gen() % size;
+    int new_val = static_cast<int>(data[idx]) + dist(gen);
+    data[idx] = static_cast<uint8_t>(std::clamp(new_val, 0, 255));
+  }
+}
+
 void AnalyserHandler::SetFftSize(unsigned size,
                                  ExceptionState& exception_state) {
   if (!IsValidFFTSize(size)) {
@@ -234,6 +264,10 @@ void AnalyserNode::getFloatFrequencyData(NotShared<DOMFloat32Array> array) {
   if (!array)
     return;
   analyser_handler_->GetFloatFrequencyData(array->Data(), array->length());
+
+  // Apply audio fingerprint protection
+  AddAudioNoise(array->Data(), array->length());
 }

 void AnalyserNode::getByteFrequencyData(NotShared<DOMUint8Array> array) {
@@ -241,6 +275,10 @@ void AnalyserNode::getByteFrequencyData(NotShared<DOMUint8Array> array) {
   if (!array)
     return;
   analyser_handler_->GetByteFrequencyData(array->Data(), array->length());
+
+  // Apply audio fingerprint protection
+  AddByteNoise(array->Data(), array->length());
 }

 void AnalyserNode::getFloatTimeDomainData(NotShared<DOMFloat32Array> array) {
@@ -248,6 +286,10 @@ void AnalyserNode::getFloatTimeDomainData(NotShared<DOMFloat32Array> array) {
   if (!array)
     return;
   analyser_handler_->GetFloatTimeDomainData(array->Data(), array->length());
+
+  // Apply audio fingerprint protection
+  AddAudioNoise(array->Data(), array->length());
 }

 void AnalyserNode::getByteTimeDomainData(NotShared<DOMUint8Array> array) {
@@ -255,6 +297,10 @@ void AnalyserNode::getByteTimeDomainData(NotShared<DOMUint8Array> array) {
   if (!array)
     return;
   analyser_handler_->GetByteTimeDomainData(array->Data(), array->length());
+
+  // Apply audio fingerprint protection
+  AddByteNoise(array->Data(), array->length());
 }

 }  // namespace blink

diff --git a/third_party/blink/renderer/modules/webaudio/oscillator_node.cc b/third_party/blink/renderer/modules/webaudio/oscillator_node.cc
index bcdef12..2345678 100644
--- a/third_party/blink/renderer/modules/webaudio/oscillator_node.cc
+++ b/third_party/blink/renderer/modules/webaudio/oscillator_node.cc
@@ -32,6 +32,7 @@
 #include "third_party/blink/renderer/modules/webaudio/periodic_wave.h"
 #include "third_party/blink/renderer/platform/audio/audio_utilities.h"
 #include "third_party/blink/renderer/platform/bindings/exception_state.h"
+#include <random>

 namespace blink {

@@ -178,6 +179,20 @@ bool OscillatorHandler::PropagatesSilence() const {
   return !IsPlayingOrScheduled() || HasFinished();
 }

+// Audio Fingerprint Protection: Add frequency variation
+static float GetFrequencyNoise() {
+  static thread_local std::mt19937_64 gen(
+      std::chrono::system_clock::now().time_since_epoch().count());
+  std::uniform_real_distribution<float> dist(-0.00001f, 0.00001f);
+  return dist(gen);
+}
+
+static float GetPhaseNoise() {
+  static thread_local std::mt19937_64 gen(
+      std::chrono::system_clock::now().time_since_epoch().count());
+  std::uniform_real_distribution<float> dist(-0.0001f, 0.0001f);
+  return dist(gen);
+}

 void OscillatorHandler::Process(uint32_t frames_to_process) {
   AudioBus* output_bus = Output(0).Bus();
@@ -215,6 +230,9 @@ void OscillatorHandler::Process(uint32_t frames_to_process) {
   for (unsigned i = 0; i < frames_to_process; ++i) {
     float frequency = narrow_cast<float>(frequency_values[i]);

+    // Apply imperceptible frequency variation for fingerprint protection
+    frequency *= (1.0f + GetFrequencyNoise());
+
     // Calculate the sample value using the phase
     float sample;
     if (type_ == OscillatorType::kSine) {
@@ -235,6 +253,9 @@ void OscillatorHandler::Process(uint32_t frames_to_process) {

     dest_p[i] = sample;

+    // Add imperceptible phase noise
+    phase_ += GetPhaseNoise();
+
     // Increment phase
     phase_ += incr;
     if (phase_ >= 1.0) {

diff --git a/third_party/blink/renderer/modules/webaudio/dynamics_compressor_node.cc b/third_party/blink/renderer/modules/webaudio/dynamics_compressor_node.cc
index cdef123..3456789 100644
--- a/third_party/blink/renderer/modules/webaudio/dynamics_compressor_node.cc
+++ b/third_party/blink/renderer/modules/webaudio/dynamics_compressor_node.cc
@@ -30,6 +30,7 @@
 #include "third_party/blink/renderer/modules/webaudio/base_audio_context.h"
 #include "third_party/blink/renderer/platform/audio/dynamics_compressor.h"
 #include "third_party/blink/renderer/platform/bindings/exception_state.h"
+#include <random>

 namespace blink {

@@ -156,8 +157,21 @@ void DynamicsCompressorHandler::Process(uint32_t frames_to_process) {
                               frames_to_process);
 }

+// Audio Fingerprint Protection: Add reduction noise
+static float GetReductionNoise() {
+  static thread_local std::mt19937_64 gen(
+      std::chrono::system_clock::now().time_since_epoch().count());
+  std::uniform_real_distribution<float> dist(-0.001f, 0.001f);
+  return dist(gen);
+}
+
 float DynamicsCompressorHandler::ReductionValue() const {
-  return reduction_;
+  // Apply subtle noise to reduction value for fingerprint protection
+  float reduction = reduction_;
+  if (reduction != 0.0f) {
+    reduction *= (1.0f + GetReductionNoise());
+  }
+  return reduction;
 }

 void DynamicsCompressorHandler::SetChannelCount(

diff --git a/third_party/blink/renderer/modules/webaudio/offline_audio_context.cc b/third_party/blink/renderer/modules/webaudio/offline_audio_context.cc
index def1234..4567890 100644
--- a/third_party/blink/renderer/modules/webaudio/offline_audio_context.cc
+++ b/third_party/blink/renderer/modules/webaudio/offline_audio_context.cc
@@ -34,6 +34,7 @@
 #include "third_party/blink/renderer/platform/audio/audio_utilities.h"
 #include "third_party/blink/renderer/platform/bindings/exception_state.h"
 #include "third_party/blink/renderer/platform/heap/persistent.h"
+#include <random>

 namespace blink {

@@ -189,6 +190,26 @@ void OfflineAudioContext::DidSuspend(ScriptPromiseResolver* resolver) {
   resolver->Resolve();
 }

+// Audio Fingerprint Protection: Add noise to rendered audio buffer
+static void AddBufferNoise(AudioBuffer* buffer) {
+  if (!buffer)
+    return;
+
+  static thread_local std::mt19937_64 gen(
+      std::chrono::system_clock::now().time_since_epoch().count());
+  std::uniform_real_distribution<float> dist(-0.00001f, 0.00001f);
+
+  for (unsigned channel = 0; channel < buffer->numberOfChannels(); ++channel) {
+    DOMFloat32Array* channel_data = buffer->getChannelData(channel);
+    if (!channel_data)
+      continue;
+
+    float* data = channel_data->Data();
+    for (size_t i = 0; i < channel_data->length(); ++i) {
+      data[i] += dist(gen);
+    }
+  }
+}

 void OfflineAudioContext::DidFinishRendering(AudioBuffer* rendered_buffer) {
   DCHECK(IsMainThread());
@@ -201,6 +222,9 @@ void OfflineAudioContext::DidFinishRendering(AudioBuffer* rendered_buffer) {
   // Return early if the rendering was aborted.
   if (!is_rendering_started_)
     return;
+
+  // Apply audio fingerprint protection
+  AddBufferNoise(rendered_buffer);

   // All promises are resolved at once. If the result is null, they will all
   // reject.

diff --git a/third_party/blink/renderer/modules/webaudio/audio_destination_node.cc b/third_party/blink/renderer/modules/webaudio/audio_destination_node.cc
index ef12345..5678901 100644
--- a/third_party/blink/renderer/modules/webaudio/audio_destination_node.cc
+++ b/third_party/blink/renderer/modules/webaudio/audio_destination_node.cc
@@ -29,6 +29,7 @@
 #include "third_party/blink/renderer/modules/webaudio/audio_context.h"
 #include "third_party/blink/renderer/modules/webaudio/base_audio_context.h"
 #include "third_party/blink/renderer/platform/audio/denormal_disabler.h"
+#include <random>

 namespace blink {

@@ -89,6 +90,23 @@ void AudioDestinationHandler::SetSampleRate(float sample_rate) {
   sample_rate_ = sample_rate;
 }

+// Audio Fingerprint Protection: Add imperceptible output noise
+static void AddOutputNoise(AudioBus* bus) {
+  if (!bus)
+    return;
+
+  static thread_local std::mt19937_64 gen(
+      std::chrono::system_clock::now().time_since_epoch().count());
+  std::uniform_real_distribution<float> dist(-0.000001f, 0.000001f);
+
+  for (unsigned channel = 0; channel < bus->NumberOfChannels(); ++channel) {
+    float* channel_data = bus->Channel(channel)->MutableData();
+    for (size_t i = 0; i < bus->length(); ++i) {
+      channel_data[i] += dist(gen);
+    }
+  }
+}
+
 void AudioDestinationHandler::Render(AudioBus* source_bus,
                                      AudioBus* destination_bus,
                                      uint32_t frames_to_process,
@@ -105,6 +123,9 @@ void AudioDestinationHandler::Render(AudioBus* source_bus,
   // This handles the case of the audio context (realtime or offline) being
   // suspended or closed.
   Context()->HandlePostRenderTasks(source_bus);
+
+  // Apply audio fingerprint protection (imperceptible level)
+  AddOutputNoise(destination_bus);
 }

 // ----------------------------------------------------------------
