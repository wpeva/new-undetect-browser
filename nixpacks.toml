[phases.setup]
nixPkgs = ['nodejs-20_x']

[phases.install]
cmds = [
  'npm install --legacy-peer-deps'
]

[phases.build]
cmds = [
  'npx tsc --skipLibCheck || npx tsc'
]

[start]
cmd = 'node dist/server/index-v2.js'
